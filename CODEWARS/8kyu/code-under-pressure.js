/*
Code as fast as you can! You need to double the integer and return it.
*/

function doubleInteger(i) {
    // Multiply the input integer by 2
    const doubledValue = i * 2;
  
    // Return the doubled value
    return doubledValue;
  }
  
    // end.